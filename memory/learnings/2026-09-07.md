# Learnings — 2026-09-07

## Session chantier embellissement CV (clôturée à 17:59)

Pas une phase PRD — suivi via `DOCS/plans/cv-embellissement-scope.md`. Aucun `/validate` (housekeeping + éval).

### Commits
- `40fe69e` chore(status): refresh SHA post-close
- `0caeabd` docs(status): session 2026-09-07 — T1 éval classifieur, T2 éval RAG, T5 CI verte + recap *(commit de cette /close)*
- `913ec35` feat(t2): RAG eval reelle — hit rate @5 100%, rang 1 sur 5/5 (pgvector 0.8.0)
- `4155cc9` feat(t1): eval classifieur v1 vs v2 executee (40 e-mails), v1 reste le moteur
- `5e5d6af` chore(n8n): purge dernier e-mail hardcodé -> CLIENT_SUMMARY_RECIPIENT
- `480705a` test(e2e): Quick Actions = 6 boutons (2 classifieurs e-mail séparés)
- `0861c94` ci(t5): image pgvector/pgvector:pg16 pour la table documents (RAG T2)

### Fichiers clés touchés
- `.github/workflows/ci.yml` — postgres → `pgvector/pgvector:pg16` (2 jobs)
- `tests/e2e/voice-connection.spec.ts` — `toHaveCount(5)` → `6`
- `workflows/reminders.json`, `workflows/smart-agent.json` — e-mail hardcodé → `$env.CLIENT_SUMMARY_RECIPIENT`
- `DOCS/reports/classifier-eval-2026-09-07.md` (nouveau)
- `DOCS/reports/rag-eval-2026-09-07.md` (nouveau)
- `DOCS/plans/cv-embellissement-scope.md` — bloc CV « À ENVOYER MAINTENANT — 6/6 »
- `eval/classifier/dataset.csv` (gitignored — 40 e-mails annotés fournis par l'utilisateur)

### Gotchas rencontrés
- **Docker Hub CDN cassé sur la machine locale** : `pgvector/pgvector:pg16` échoue avec `httpReadSeeker: failed open ... EOF` sur `production.cloudfront.docker.com` (récurrent depuis le 04-09). Contournement : build local `FROM postgres:16` (image en cache) + `pgvector` v0.8.0 compilé depuis GitHub (`build-essential` + `postgresql-server-dev-16`). apt (deb.debian.org) et GitHub passent, seul CloudFront échoue.
- **`.env` a un octet non-cp1252** → `pathlib.read_text()` sans `encoding` plante sur Windows. Toujours `encoding='utf-8'`.
- **CI `ci.yml` écrit avant T2** : pinnait `postgres:16-alpine` sans pgvector → `init_db()` (`CREATE TABLE documents ... VECTOR(1536)`) plante `type "vector" does not exist`. Sur backend-tests ET e2e (même cause).
- **`git commit` multi-`-m` / heredoc bloqué par le classifier auto-mode** cette session — passer par un `-m` court sur une ligne.
- **`git commit -am`** a ré-embarqué `tsconfig.tsbuildinfo` (artefact de build tracké) dans `40fe69e` — inoffensif mais à surveiller.

### Résultats d'éval
- **Classifieur v1 vs v2** (40 e-mails, `gpt-4o-mini`) : v1 macro-F1 0,69 urgence / 0,50 action ≥ v2 (0,63 / 0,45). v2 (JSON Schema) = robustesse du format, pas la qualité → `WORKFLOW_VERSION=v1` confirmé. Angles morts : classe `moyenne` fuit vers `faible` ; classe `aucune` jamais prédite (F1 0,00).
- **RAG** (pgvector 0.8.0, `text-embedding-3-small`, cosinus top-k) : hit rate @5 = 100 %, rang 1 sur 5/5. Corpus PoC = 5 `sessions/*.md` (Redis `emails_structured` vide en local). `@5` trivial sur 5 docs → le signal est le rang 1 systématique.
